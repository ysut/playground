services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vite-container
    ports:
      - "5505:80"
    volumes:
      - type: bind
        source: ../
        target: /workspace
    stdin_open: true
    tty: true